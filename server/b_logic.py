import database as db


def all_mock_ops():
    """
    starting point for you
    call the db.get(mock_ops, [lsit of columns (specify from abbreviation, name, lat, long, didok_id)], optional=Filters"""
    # process the data into dictionary format and return
    return data